# QA+ Company OS 개발 지침

- 이 저장소는 QA+ 콘텐츠·품질관리 업무를 한 화면에서 운영하는 로컬 우선 Company OS다.
- 작업 전 현재 상태와 사용자 데이터를 확인하고 기존 `data/os_state.json`을 보존한다.
- 외부 게시·메시지 전송·배포는 대표님의 명시적 요청과 승인 없이 실행하지 않는다.
- API 키, OAuth 토큰, 개인정보는 코드·로그·상태 파일·화면에 저장하거나 노출하지 않는다.
- 법령, HACCP/FSSC22000 수치와 한계기준은 공식 출처를 확인하고 추측하지 않는다.
- UI 변경은 실제 브라우저에서 데스크톱과 모바일 화면을 확인한다.
- 빌드 성공만으로 완료 처리하지 말고, 데이터 저장·새로고침 유지·산출물 열기까지 검증한다.
- 1차 핵심 루프는 `주제 입력 → 생성 → 검수 → 대표 승인 → 로컬 저장`이다.
- 승인 전 외부 발행 차단은 하드 게이트로 유지한다.
